import { NextResponse } from 'next/server';
const origin = () => process.env.AUTH_SERVICE_URL ?? 'http://127.0.0.1:3005';
export async function POST(request: Request) { const upstream = await fetch(`${origin()}/auth/register`, { method: 'POST', headers: { 'content-type': 'application/json' }, body: await request.text(), cache: 'no-store' }); const body = await upstream.json(); const response = NextResponse.json(body, { status: upstream.status }); if (upstream.ok) response.cookies.set('saga_session', body.token, { httpOnly: true, sameSite: 'lax', secure: process.env.NODE_ENV === 'production', path: '/', maxAge: 604800 }); return response; }
