import { cookies } from 'next/headers';
export async function GET() { const token = (await cookies()).get('saga_session')?.value; const response = await fetch(`${process.env.AUTH_SERVICE_URL ?? 'http://127.0.0.1:3005'}/auth/session`, { headers: token ? { 'x-session-token': token } : {}, cache: 'no-store' }); return new Response(await response.text(), { status: response.status, headers: { 'content-type': 'application/json', 'cache-control': 'no-store' } }); }
export async function DELETE() { const token = (await cookies()).get('saga_session')?.value; await fetch(`${process.env.AUTH_SERVICE_URL ?? 'http://127.0.0.1:3005'}/auth/logout`, { method: 'POST', headers: token ? { 'x-session-token': token } : {}, cache: 'no-store' }); const response = new Response(null, { status: 204 }); response.headers.append('Set-Cookie', 'saga_session=; Path=/; HttpOnly; SameSite=Lax; Max-Age=0'); return response; }
