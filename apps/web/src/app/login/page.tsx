"use client";
import { useState, type FormEvent } from "react";
import { useRouter } from "next/navigation";
import { PageHeading } from "@/components/layout/page-heading";
import { Card } from "@/components/ui/card";
import { Input } from "@/components/ui/input";
import { Button, ButtonLink } from "@/components/ui/button";
export default function LoginPage() { const [error,setError]=useState(""); const [pending,setPending]=useState(false); const router=useRouter(); async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setPending(true);setError("");const f=new FormData(e.currentTarget);const r=await fetch("/api/auth/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email:f.get("email"),password:f.get("password")})});if(r.ok)router.push("/");else setError((await r.json()).error??"Unable to sign in.");setPending(false);} return <><PageHeading eyebrow="SECURE ACCESS" title="Welcome back" description="Sign in to manage orders, payment approvals, and your account."/><div className="auth-layout"><Card className="auth-card"><form onSubmit={submit} className="auth-form"><h2>Sign in</h2><p className="field-hint">Use your workspace email and password.</p><Input label="Email address" name="email" type="email" autoComplete="email" required/><Input label="Password" name="password" type="password" autoComplete="current-password" minLength={12} required/>{error&&<p className="field-error" role="alert">{error}</p>}<Button type="submit" disabled={pending}>{pending?"Signing in…":"Sign in"}</Button><p className="auth-switch">New here? <ButtonLink href="/register" variant="ghost">Create an account</ButtonLink></p></form></Card><aside className="auth-aside"><p className="eyebrow">ORDER OPERATIONS</p><h2>One secure workspace.</h2><p>Track orders, inspect Saga history, and access payment controls according to your role.</p></aside></div></>; }
